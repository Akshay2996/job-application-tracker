"use client";

import { Board } from "@/lib/models/model.types";

interface KanbanBoardProps {
  board: Board;
  userId: string;
}

export default function KanbanBoard({ board, userId }: KanbanBoardProps) {
  return <></>;
}
